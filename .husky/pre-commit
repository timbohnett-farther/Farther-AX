#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

# Pre-commit quality gates

# Run type check (must pass with no errors)
npx tsc --noEmit || exit 1

echo "✓ Pre-commit checks passed (TypeScript compilation OK)"
exit 0
